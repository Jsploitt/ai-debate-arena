name: Revalidate runtime sync

on:
  push:
    branches:
      - agent/fix-runtime-sync-handoff

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Typecheck
        if: always()
        run: bunx tsc --noEmit
      - name: Lint
        if: always()
        run: bun run lint
      - name: Build
        if: always()
        run: bun run build
