@AGENTS.md

## Claude Code

- For tasks spanning more than one area, start with the `hub` agent under `.claude/agents/`.
- Preserve the repository invariants imported from `AGENTS.md`.
- Use the repository's existing skills and commands instead of recreating project knowledge ad hoc.
- Before completing implementation work, run the appropriate verification workflow; use `/verify` for the full quality gate.
